// import { isAuthenticated } from 'src/domains/config/index'
// import { hasPermission } from 'src/feature/permissions'
//
// export default function permissionPolicy (verb, noun) {
//   return () => isAuthenticated() && hasPermission(verb, noun)
// }
